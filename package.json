{
  "name": "ai-ability-radar",
  "version": "0.2.2",
  "private": true,
  "license": "Apache-2.0",
  "allowScripts": {
    "esbuild@0.28.1": true
  },
  "workspaces": [
    "apps/desktop"
  ],
  "devDependencies": {
    "typescript": "5.8.3"
  },
  "scripts": {
    "build": "npm run build --workspace apps/desktop",
    "test": "cargo build -p ability-core --bin ability-pack-validator --locked --offline && node --test scripts/package-portable.test.mjs && node --test scripts/repository-contracts.test.mjs && npm run test --workspace apps/desktop",
    "start": "npm run tauri -- dev",
    "package:portable": "npm run tauri -- build --no-bundle && npm run package:portable:from-build",
    "package:portable:from-build": "cargo build -p ability-core --bin ability-pack-validator --release --locked --offline && node scripts/package-portable.mjs",
    "tauri": "npm run tauri --workspace apps/desktop --",
    "validate:repository": "node scripts/validate-repository.mjs",
    "licenses:generate": "node scripts/generate-license-metadata.mjs"
  }
}
